	.text
	
	lui $t1,0x17
	ori $t1,0x73
	lui $t2,0x62
	ori $t2,0x69
	lui $t3,0x56
	ori $t3,0x93
	mult $t1,$t2
	mfhi $s1
	sw $s1,($a0)
	mflo $s2
	addi $a0,$a0,4
	sw $s2,($a0)
	addi $a0,$a0,4
	add $t2,$t2,$t3
	multu $t2,$t2
	sub $t2,$t2,$t1
	sw $t2,($a0)
	addi $a0,$a0,4
	mfhi $t4
	and $t2,$t4,$t2
	sw $t2,($a0)
	mflo $t4
	addi $a0,$a0,4
	xor $t2,$t2,$t4
	sw $t2,($a0)
	addi $a0,$a0,4
	
	div $t1,$t2
	mfhi $s1
	sw $s1,($a0)
	mflo $s2
	addi $a0,$a0,4
	sw $s2,($a0)
	addi $a0,$a0,4
	sllv $t2,$t2,$t3
	divu $t2,$t3
	sub $t2,$t2,$t1
	sw $t2,($a0)
	addi $a0,$a0,4
	mfhi $t4
	and $t2,$t4,$t2
	sw $t2,($a0)
	mflo $t4
	addi $a0,$a0,4
	xor $t2,$t2,$t4
	sw $t2,($a0)
	addi $a0,$a0,4
	
	sub $t5,$t1,$t2
	mthi $t5
	lw $t6,-12($a0)
	mtlo $t6
	mflo $t7
	mfhi $t8
	sw $t7,($a0)
	addi $a0,$a0,4
	sw $t8,($a0)
	addi $a0,$a0,4
	
	and $t5,$t3,$t4
	lw $t6,($t0)
	mtlo $t5
	mthi $t6
	mfhi $t7
	mflo $t8
	sw $t7,($a0)
	addi $a0,$a0,4
	sw $t8,($a0)
	addi $a0,$a0,4	
